info:
  name: Crear especialidad
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/v1/especialidades
  body:
    type: json
    data: |-
      {
        "nombre": "CARDILOGÍA"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
