info:
  name: Todas
  type: http

http:
  method: GET
  url: http://localhost:3000/api/v1/especialidades
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
